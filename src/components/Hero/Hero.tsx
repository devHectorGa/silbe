import { Typography } from "antd";
import { HeroContainer } from "./Hero.styles";

export const Hero = () => {
  return (
    <HeroContainer>
      <Typography.Title>Título</Typography.Title>
    </HeroContainer>
  );
};
